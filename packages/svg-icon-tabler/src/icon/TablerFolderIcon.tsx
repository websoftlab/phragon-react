import type { SvgIconExternalProps } from "@phragon-react/svg-icon";
import type { SVGProps } from "react";
import { SvgIcon } from "@phragon-react/svg-icon";
import React from "react";
import { __isDev__ } from "@phragon-util/global-var";

export interface TablerFolderIconProps extends SVGProps<SVGSVGElement>, SvgIconExternalProps {}

export const TablerFolderIcon = React.forwardRef<SVGSVGElement, TablerFolderIconProps>(function TablerFolderIcon(
	props,
	ref
) {
	const { children, ...rest } = props;
	return (
		<SvgIcon
			stroke="currentColor"
			strokeWidth={2}
			strokeLinecap="round"
			strokeLinejoin="round"
			{...rest}
			ref={ref}
			size={24}
			icon={undefined}
			fill="none"
		>
			<path d="M5 4h4l3 3h7a2 2 0 0 1 2 2v8a2 2 0 0 1 -2 2h-14a2 2 0 0 1 -2 -2v-11a2 2 0 0 1 2 -2" />
			{children}
		</SvgIcon>
	);
});

if (__isDev__()) {
	TablerFolderIcon.displayName = "TablerFolderIcon";
}
