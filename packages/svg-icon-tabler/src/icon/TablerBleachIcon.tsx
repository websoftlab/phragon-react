import type { SvgIconExternalProps } from "@phragon-react/svg-icon";
import type { SVGProps } from "react";
import { SvgIcon } from "@phragon-react/svg-icon";
import React from "react";
import { __isDev__ } from "@phragon-util/global-var";

export interface TablerBleachIconProps extends SVGProps<SVGSVGElement>, SvgIconExternalProps {}

export const TablerBleachIcon = React.forwardRef<SVGSVGElement, TablerBleachIconProps>(function TablerBleachIcon(
	props,
	ref
) {
	const { children, ...rest } = props;
	return (
		<SvgIcon
			stroke="currentColor"
			strokeWidth={2}
			strokeLinecap="round"
			strokeLinejoin="round"
			{...rest}
			ref={ref}
			size={24}
			icon={undefined}
			fill="none"
		>
			<path d="M5 19h14a2 2 0 0 0 1.84 -2.75l-7.1 -12.25a2 2 0 0 0 -3.5 0l-7.1 12.25a2 2 0 0 0 1.75 2.75" />
			{children}
		</SvgIcon>
	);
});

if (__isDev__()) {
	TablerBleachIcon.displayName = "TablerBleachIcon";
}
