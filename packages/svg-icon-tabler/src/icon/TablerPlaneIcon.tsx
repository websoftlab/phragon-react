import type { SvgIconExternalProps } from "@phragon-react/svg-icon";
import type { SVGProps } from "react";
import { SvgIcon } from "@phragon-react/svg-icon";
import React from "react";
import { __isDev__ } from "@phragon-util/global-var";

export interface TablerPlaneIconProps extends SVGProps<SVGSVGElement>, SvgIconExternalProps {}

export const TablerPlaneIcon = React.forwardRef<SVGSVGElement, TablerPlaneIconProps>(function TablerPlaneIcon(
	props,
	ref
) {
	const { children, ...rest } = props;
	return (
		<SvgIcon
			stroke="currentColor"
			strokeWidth={2}
			strokeLinecap="round"
			strokeLinejoin="round"
			{...rest}
			ref={ref}
			size={24}
			icon={undefined}
			fill="none"
		>
			<path d="M16 10h4a2 2 0 0 1 0 4h-4l-4 7h-3l2 -7h-4l-2 2h-3l2 -4l-2 -4h3l2 2h4l-2 -7h3z" />
			{children}
		</SvgIcon>
	);
});

if (__isDev__()) {
	TablerPlaneIcon.displayName = "TablerPlaneIcon";
}
