import type { SvgIconExternalProps } from "@phragon-react/svg-icon";
import type { SVGProps } from "react";
import { SvgIcon } from "@phragon-react/svg-icon";
import React from "react";
import { __isDev__ } from "@phragon-util/global-var";

export interface TablerBetaIconProps extends SVGProps<SVGSVGElement>, SvgIconExternalProps {}

export const TablerBetaIcon = React.forwardRef<SVGSVGElement, TablerBetaIconProps>(function TablerBetaIcon(props, ref) {
	const { children, ...rest } = props;
	return (
		<SvgIcon
			stroke="currentColor"
			strokeWidth={2}
			strokeLinecap="round"
			strokeLinejoin="round"
			{...rest}
			ref={ref}
			size={24}
			icon={undefined}
			fill="none"
		>
			<path d="M8 22v-14a4 4 0 0 1 4 -4h.5a3.5 3.5 0 0 1 0 7h-.5h.5a4.5 4.5 0 1 1 -4.5 4.5v-.5" />
			{children}
		</SvgIcon>
	);
});

if (__isDev__()) {
	TablerBetaIcon.displayName = "TablerBetaIcon";
}
