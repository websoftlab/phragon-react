import type { SvgIconExternalProps } from "@phragon-react/svg-icon";
import type { SVGProps } from "react";
import { SvgIcon } from "@phragon-react/svg-icon";
import React from "react";
import { __isDev__ } from "@phragon-util/global-var";

export interface TablerTrackIconProps extends SVGProps<SVGSVGElement>, SvgIconExternalProps {}

export const TablerTrackIcon = React.forwardRef<SVGSVGElement, TablerTrackIconProps>(function TablerTrackIcon(
	props,
	ref
) {
	const { children, ...rest } = props;
	return (
		<SvgIcon
			stroke="currentColor"
			strokeWidth={2}
			strokeLinecap="round"
			strokeLinejoin="round"
			{...rest}
			ref={ref}
			size={24}
			icon={undefined}
			fill="none"
		>
			<path d="M4 15l11 -11m5 5l-11 11m-4 -8l7 7m-3.5 -10.5l7 7m-3.5 -10.5l7 7" />
			{children}
		</SvgIcon>
	);
});

if (__isDev__()) {
	TablerTrackIcon.displayName = "TablerTrackIcon";
}
