import type { SvgIconExternalProps } from "@phragon-react/svg-icon";
import type { SVGProps } from "react";
import { SvgIcon } from "@phragon-react/svg-icon";
import React from "react";
import { __isDev__ } from "@phragon-util/global-var";

export interface TablerSumIconProps extends SVGProps<SVGSVGElement>, SvgIconExternalProps {}

export const TablerSumIcon = React.forwardRef<SVGSVGElement, TablerSumIconProps>(function TablerSumIcon(props, ref) {
	const { children, ...rest } = props;
	return (
		<SvgIcon
			stroke="currentColor"
			strokeWidth={2}
			strokeLinecap="round"
			strokeLinejoin="round"
			{...rest}
			ref={ref}
			size={24}
			icon={undefined}
			fill="none"
		>
			<path d="M18 16v2a1 1 0 0 1 -1 1h-11l6 -7l-6 -7h11a1 1 0 0 1 1 1v2" />
			{children}
		</SvgIcon>
	);
});

if (__isDev__()) {
	TablerSumIcon.displayName = "TablerSumIcon";
}
