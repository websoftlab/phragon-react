import type { SvgIconExternalProps } from "@phragon-react/svg-icon";
import type { SVGProps } from "react";
import { SvgIcon } from "@phragon-react/svg-icon";
import React from "react";
import { __isDev__ } from "@phragon-util/global-var";

export interface TablerScribbleIconProps extends SVGProps<SVGSVGElement>, SvgIconExternalProps {}

export const TablerScribbleIcon = React.forwardRef<SVGSVGElement, TablerScribbleIconProps>(function TablerScribbleIcon(
	props,
	ref
) {
	const { children, ...rest } = props;
	return (
		<SvgIcon
			stroke="currentColor"
			strokeWidth={2}
			strokeLinecap="round"
			strokeLinejoin="round"
			{...rest}
			ref={ref}
			size={24}
			icon={undefined}
			fill="none"
		>
			<path d="M3 15c2 3 4 4 7 4s7 -3 7 -7s-3 -7 -6 -7s-5 1.5 -5 4s2 5 6 5s8.408 -2.453 10 -5" />
			{children}
		</SvgIcon>
	);
});

if (__isDev__()) {
	TablerScribbleIcon.displayName = "TablerScribbleIcon";
}
