import type { SvgIconExternalProps } from "@phragon-react/svg-icon";
import type { SVGProps } from "react";
import { SvgIcon } from "@phragon-react/svg-icon";
import React from "react";
import { __isDev__ } from "@phragon-util/global-var";

export interface TablerBellIconProps extends SVGProps<SVGSVGElement>, SvgIconExternalProps {}

export const TablerBellIcon = React.forwardRef<SVGSVGElement, TablerBellIconProps>(function TablerBellIcon(props, ref) {
	const { children, ...rest } = props;
	return (
		<SvgIcon
			stroke="currentColor"
			strokeWidth={2}
			strokeLinecap="round"
			strokeLinejoin="round"
			{...rest}
			ref={ref}
			size={24}
			icon={undefined}
			fill="none"
		>
			<path d="M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9" />
			<path d="M10.3 21a1.94 1.94 0 0 0 3.4 0" />
			{children}
		</SvgIcon>
	);
});

if (__isDev__()) {
	TablerBellIcon.displayName = "TablerBellIcon";
}
